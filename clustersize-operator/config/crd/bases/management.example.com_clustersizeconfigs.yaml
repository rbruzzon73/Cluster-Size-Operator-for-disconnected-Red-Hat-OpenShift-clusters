---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: clustersizeconfigs.management.example.com
spec:
  group: management.example.com
  names:
    kind: ClusterSizeConfig
    listKind: ClusterSizeConfigList
    plural: clustersizeconfigs
    singular: clustersizeconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterSizeConfig is the Schema for the clustersizeconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSizeConfigSpec defines the desired state of ClusterSizeConfig
            properties:
              checkInterval:
                type: string
              isBareMetal:
                description: |-
                  IsBareMetal specifies whether the cluster runs on raw physical hardware.
                  This parameter acts as a fallback for UPI bare metal configurations.
                type: boolean
              logMaxRotations:
                type: integer
              logMaxSizeBytes:
                type: integer
              remoteIp:
                type: string
              remoteUdpPort:
                type: integer
              secret:
                type: string
              subscriptionServiceLevel:
                description: SubscriptionServiceLevel dictates the contract service
                  tier associated with the cluster.
                enum:
                - Premium
                - Standard
                type: string
              suspend:
                type: boolean
            required:
            - checkInterval
            - isBareMetal
            - remoteIp
            - remoteUdpPort
            - secret
            - subscriptionServiceLevel
            type: object
          status:
            description: ClusterSizeConfigStatus defines the observed state of ClusterSizeConfig
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
