---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: clustersizeconfigs.management.example.com
spec:
  group: management.example.com
  names:
    kind: ClusterSizeConfig
    listKind: ClusterSizeConfigList
    plural: clustersizeconfigs
    singular: clustersizeconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterSizeConfig is the Schema for the clustersizeconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSizeConfigSpec defines the desired state of ClusterSizeConfig
            properties:
              checkInterval:
                description: |-
                  CheckInterval defines the frequency of telemetry checks.
                  It supports pure numbers for hours (e.g., "10") or explicit units (e.g., "24h", "36000s", "90m").
                type: string
              isBareMetal:
                default: false
                description: |-
                  IsBareMetal specifies whether the cluster runs on native physical hardware.
                  This parameter acts as a fallback for bare metal UPI configurations.
                type: boolean
              logMaxRotations:
                description: LogMaxRotations defines the maximum number of log file
                  rotations to keep.
                type: integer
              logMaxSizeBytes:
                description: LogMaxSizeBytes defines the maximum size in bytes before
                  the log file is rotated.
                type: integer
              remoteIp:
                description: RemoteIp is the IP address of the central aggregation
                  server to send the UDP payload to.
                type: string
              remoteUdpPort:
                description: RemoteUdpPort is the UDP port of the central aggregation
                  server (e.g., 555).
                type: integer
              secret:
                description: |-
                  Secret is the name of the Kubernetes Secret containing the cryptographic key (salt) for hashing.
                  If the specified Secret does not exist, the operator will automatically create it with a default value the first time.
                type: string
              subscriptionServiceLevel:
                description: SubscriptionServiceLevel defines the contract level associated
                  with the cluster (Premium or Standard).
                enum:
                - Premium
                - Standard
                type: string
              suspend:
                description: Suspend allows temporarily suspending the monitoring
                  cycle execution if set to true.
                type: boolean
            required:
            - checkInterval
            - remoteIp
            - remoteUdpPort
            - secret
            - subscriptionServiceLevel
            type: object
          status:
            description: ClusterSizeConfigStatus defines the observed state of ClusterSizeConfig
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
